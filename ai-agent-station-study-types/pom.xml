<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>cn.bugstack.ai</groupId>
        <artifactId>ai-agent-station-study</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>ai-agent-station-study-types</artifactId>

    <dependencies>
        <!-- 原先声明 spring-boot-starter-web，但本模块实际零 web 使用：
             全模块外部 import 只有 spring-context（@Configuration/@Bean/@Scheduled/TaskScheduler）、
             spring-beans（DisposableBean）、spring-boot-autoconfigure（@ConditionalOnProperty）、
             spring-boot（@ConfigurationProperties/@EnableConfigurationProperties）——
             连 spring-web、servlet、jackson 都没有。
             改用 spring-boot-starter（含 spring-boot + spring-context + spring-boot-autoconfigure + logging），
             使 types 不再向消费方传递 tomcat / spring-webmvc，回归「纯类型 + 任务调度」定位。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <!-- xstream 1.4.10 与 dom4j 1.6.1 已移除：全项目 import 命中数均为 0，
             且两者历史上都有反序列化 CVE，留在依赖树里只是扩大攻击面 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>ai-agent-station-study-types</finalName>
    </build>

</project>
